class User {
  constructor(Username, Password, FullName, Gender, Birthday) {
    this.Username = Username;
    this.Password = Password;
    this.FullName = FullName;
    this.Gender = Gender;
    this.Birthday = Birthday;
  }
}

module.exports = User;
